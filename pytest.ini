[pytest]
addopts =
    --import-mode=prepend
pythonpath =
    .
testpaths =
    tests
norecursedirs =
    .venv
    .git
    .idea
    __pycache__
    build
    dist
    examples
markers =
    slow: long-running tests
    cuda: requires CUDA/CuPy and GPU extensions
